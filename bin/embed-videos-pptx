#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
IN="${1:-$ROOT/build/totalpass-2026.pptx}"
OUT="${2:-$ROOT/build/totalpass-2026.with-video.pptx}"

python "$ROOT/bin/embed_videos_pptx.py" "$IN" "$OUT"
